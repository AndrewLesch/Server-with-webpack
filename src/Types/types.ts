type DownloadPhoto = {
    albumId: number,
    id: number,
    thumbnailUrl: string,
    title: string,
}

type DowlnoadAlbums = {
    id: number,
    thumbnailUrl: string,
    title: string,
    url : string;
}